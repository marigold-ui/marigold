---
title: Text
group: Content
switchTheme: true
github: https://github.com/marigold-ui/marigold/blob/main/packages/components/src/Text/Text.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/text.mdx
caption: Component for paragraphs.
---

With the `<Text>` component you can easily add different HTML paragraph elements. It should be used when you want to write long text paragraphs.

You can add properties like `align`, `color` or `fontSize` to the `<Text>`. You also have the opportunity to add sizes and custom variants to the component.

<Preview>
  <BasicText />
</Preview>

## Usage

### Import

To import the component you just have to use this code below.

```tsx
import { Text } from '@marigold/components';
```

### AppearanceTable

<AppearanceTable component={title} />

### Props

<PropsTable
  props={[
    {
      property: 'align',
      type: 'string',
      description: 'Aligns the text.',
      default: 'none',
    },
    {
      property: 'color',
      type: 'string',
      description: 'Set the text color.',
      default: 'currentColor',
    },
    {
      property: 'cursor',
      type: 'string',
      description: 'Set the cursor for the text element.',
      default: 'text',
    },
    {
      property: 'outline',
      type: 'string',
      description: 'Set the outline for the text element.',
      default: 'none',
    },
    {
      property: 'fontSize',
      type: 'string',
      description: 'Set the font size for the text element.',
      default: 'none',
    },
    {
      property: 'fontWeight',
      type: 'string',
      description: 'Set the font weight for the text element.',
      default: 'none',
    },
    {
      property: 'fontStyle',
      type: 'string',
      description: 'Set the font style for the text element.',
      default: 'none',
    },
  ]}
/>

## Examples

### Default paragraph Text

Here you can see a typical text section.

```tsx preview file=components/Text/text-default.demo.tsx

```

### Colored Text

This `<Text>` element has the property `color` with it.

```tsx preview file=components/Text/text-color.demo.tsx

```
