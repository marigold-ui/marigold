{
  "$schema": "https://turbo.build/schema.json",

  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [".env"],
      "outputs": [
        "dist/**",
        ".next/**",
        "!.next/cache/**",
        "../../storybook-static/**"
      ]
    }
  },
  "globalDependencies": ["tsconfig.json"]
}
