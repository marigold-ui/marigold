import type { Theme } from '@marigold/components';

export const Field: Theme['components']['Field'] = {
  base: {
    display: 'flex',
    flexDirection: 'row',
    gap: 'xsmall',
    alignItems: 'baseline',
  },
};
