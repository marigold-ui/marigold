import type { Theme } from '@marigold/components';

export const Text: Theme['components']['Text'] = {
  base: {
    color: 'text',
  },
  variant: {
    bold: {
      fontWeight: 'bold',
    },
  },
};
