import type { Theme } from '@marigold/components';

export const Input: Theme['components']['Input'] = {
  base: {
    border: '1px solid',
    borderColor: '#aaa',
    borderRadius: 'small',
    lineHeight: 'medium',
  },
};
