import { tv, type TVReturnType } from 'tailwind-variants';
export const root: TVReturnType<any, any, never, never, any, any> = tv({
  base: 'font-body text-[13px] text-root-body bg-root-background',
});
