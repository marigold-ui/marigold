import './theme.css';
import { Badge } from './components/Badge.style';
import { root } from './root';

export { Badge };

const theme = { name: 'unicorn', components: { Badge }, root };

export default theme;
