import type { Theme } from '@marigold/components';

export const CheckboxGroup: Theme['components']['CheckboxGroup'] = {
  base: {
    container: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'xxsmall',
    },
  },
};
