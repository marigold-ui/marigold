---
'@marigold/components': major
'@marigold/theme-rui': major
'@marigold/system': major
'@marigold/docs': major
---

refactor([DST-1283]): **Breaking Change** — Remove `<Multiselect>` (and the `react-select` dependency) from `@marigold/components`.

Use `<TagField>` instead.
