---
'@marigold/components': minor
'@marigold/system': minor
'@marigold/theme-rui': minor
---

feat(DST-1641): add `ErrorState`, the error sibling of `EmptyState`: same anatomy (`title`, `description`, `action`, `headingLevel`), plus a compact size for region-tier use and typed DOM passthrough (`role`, `tabIndex`, `ref`) for error-boundary fallbacks.
