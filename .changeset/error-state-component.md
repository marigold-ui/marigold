---
'@marigold/components': minor
'@marigold/system': minor
'@marigold/theme-rui': minor
---

Add `ErrorState`, the error sibling of `EmptyState`: same anatomy (`title`, `description`, `action`, `headingLevel`), plus a compact size for region-tier use and typed DOM passthrough (`role`, `tabIndex`, `ref`) for error-boundary fallbacks.
