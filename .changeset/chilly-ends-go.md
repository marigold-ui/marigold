---
'@marigold/cli': minor
---

feat(DST-1475): `marigold validate` to check a component file against the design system's rules — static (AST) and rendered (DOM/a11y) passes, reporting errors and warnings an agent or human can act on.
