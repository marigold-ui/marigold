---
'@marigold/docs': patch
---

docs(DST-1687): document the data-entry grid pattern

Extends the table record management pattern from three options to four with the
data-entry grid — a `<Table>` of always-on form controls with
`keyboardNavigationBehavior="tab"` — and restructures the Table component page's
editing guidance into `Editing`, covering editable cells and the grid side by side.
