---
"@marigold/components": patch
"@marigold/system": patch
"@marigold/theme-rui": patch
---

[DSTSUP-191]: Add a new prop `loaderType` which is by default `xloader`. New option `cycle` which shows a spinning cycle.
