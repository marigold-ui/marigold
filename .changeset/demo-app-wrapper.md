---
"@marigold/docs": patch
---

Add unified demo app at `/examples` that consolidates all pattern demos (filter, form, inventory) and app-shell navigation into a single application experience. Features a config-driven shared shell layout with sidebar navigation, section switching via menu with icons, and per-page links back to corresponding pattern guidelines. Removes all standalone pattern pages in favor of colocated demo routes.
