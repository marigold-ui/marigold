---
'@marigold/theme-rui': patch
---

Refined elevation shadow tokens: `shadow-elevation-border` is slightly more subtle with symmetric spread; `shadow-elevation-raised` softens the crisp first layer and is a touch darker overall.
