---
"create-marigold-app": minor
---

New package: Claude Code plugin for scaffolding Marigold apps. Install via `claude plugin marketplace add github:marigold-ui/marigold --sparse packages/create-marigold-app` then `claude plugin install create-marigold-app`.
