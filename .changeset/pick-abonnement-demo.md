---
'@marigold/docs': patch
---

docs(DST-1651): add an in-page date-pick demo to the Pick pattern

Adds a second stay-on-the-page example to "Choose the surface by collection size": assembling a season subscription (an Abonnement) from concert dates without a dialog. Complements the searchable `<TagField>` demo by showing an on-page pick over richer records, a `<SelectList>` of dates with sold-out entries disabled, a running count of what is chosen, and a verb-only commit whose count lives beside it rather than in the label. Gives the in-page tier and the commit-button reframe a live example. Docs-only.

[DST-1651](https://reservix.atlassian.net/browse/DST-1651)
