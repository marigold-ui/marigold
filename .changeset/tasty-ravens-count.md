---
'@marigold/components': patch
'@marigold/theme-rui': patch
'@marigold/system': patch
'@marigold/icons': patch
'@marigold/types': patch
'@marigold/eslint-config': patch
'@marigold/docs': patch
---

docs(DST-1314): add full data management pattern with filtering:

Add a full-page data management pattern example at `/examples/filter` demonstrating real-world venue browsing with API integration and multi-criteria filtering. The example includes a query provider using React Query, a toolbar with search and filter controls, applied filter chips for active filter display, and a data table with server-driven results. Also adds an inventory page at `/examples/inventory` showcasing multiple Marigold components in realistic Panel-based layouts, and an event form at `/examples/form` split across multiple Panel sections.
