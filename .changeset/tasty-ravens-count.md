---
'@marigold/docs': patch
---

docs([DST-1314]): add full data management pattern with filtering

Add a full-page data management pattern example at `/examples/filter` demonstrating real-world venue browsing with multi-criteria filtering. The example includes a query provider using React Query, a toolbar with search and filter controls, applied filter chips for active filter display, and a data table with paginated results.
