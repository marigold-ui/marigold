---
"@marigold/components": minor
"@marigold/theme-rui": major
---

feat(DST-1141): Update `<Card>` to use semantic spacing and add `stretch` property

- `<Card>` uses semantic spacing
- Added `stretch` in davor of `size="ful"`
- Updated test suite
- Fixed that the `<Card>` always take full width 