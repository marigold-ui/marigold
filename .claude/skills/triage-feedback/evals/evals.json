{
  "skill_name": "triage-feedback",
  "evals": [
    {
      "id": 1,
      "prompt": "/triage-feedback 5776",
      "expected_output": "PR 5776 was opened by someone else, so step 1 resolves respond-only mode. Steps 5 and 6 are skipped entirely: there is nothing to apply, nothing to commit, nothing to push and no changeset. The failure this guards against is announcing 'I will now apply 3 fixes' on a branch the user does not own.",
      "files": [],
      "expectations": [
        "The response identifies the mode as respond-only",
        "The response does not state or imply that it will apply fixes",
        "No file in the repository was edited, and no commit or push was made",
        "No changeset was proposed",
        "The triage table is rendered before any reply is posted or thread resolved",
        "The output names which sources ran, so an empty table is not mistaken for a PR with no feedback"
      ]
    },
    {
      "id": 2,
      "prompt": "/triage-feedback 5770",
      "expected_output": "PR 5770 is merged, and a merged PR stays merged, so this is a durable target rather than a sentence describing one. The previous prompt read 'on a PR that has already been merged', which named nothing: the executor had to go and find a merged PR itself, and said so.\n\nStep 1 stops on a merged or closed PR, before any gather or mode resolution. Resolving threads on a landed PR is noise and the fixes have nowhere to go.",
      "files": [],
      "expectations": [
        "The response stops and says the PR is merged or closed",
        "No reply was posted and no thread was resolved",
        "No file in the repository was edited, and no commit or push was made",
        "The response does not gather or triage the PR's feedback anyway"
      ]
    },
    {
      "id": 3,
      "prompt": "/triage-feedback --github",
      "expected_output": "Only the GitHub half is gathered. The Vercel source contributes no rows and is not gathered at all. The triage table in step 4 is the approval for every reply and resolve, so it is rendered before any of them happen, and it names that only the GitHub source ran.",
      "files": [],
      "expectations": [
        "Only GitHub review threads were gathered; no Vercel toolbar call was made",
        "The output names GitHub as the only source that ran",
        "A triage table is rendered covering every gathered item, each triaged on the same three axes",
        "No reply was posted and no thread was resolved before the table was shown",
        "Steps 1 to 3 posted nothing, resolved nothing, and edited no file in the repository"
      ]
    },
    {
      "id": 4,
      "prompt": "/triage-feedback 5777",
      "expected_output": "PRECONDITION: PR 5777 is the user's own, on branch `feat/DST-1665-listview-selection`, and that branch is NOT checked out. Run this from any other branch. If it happens to be checked out the run resolves plain author mode instead and the case is inconclusive.\n\nThe previous prompt read 'on my own PR while I have a different branch checked out', which named no PR and stated the precondition as narration rather than setup.\n\nAuthor mode, no apply: the skill asks the user to switch to the branch and re-run, and until then every apply row becomes needs-human. Replies do not need the code, fixes do. The skill must not check the branch out itself, because swapping someone's worktree is the largest side effect in the pass.",
      "files": [],
      "expectations": [
        "The response identifies the mode as author with no apply",
        "The response asks the user to switch to the branch and re-run",
        "No git checkout or branch switch was performed by the skill",
        "Every row that would otherwise be an apply is marked needs-human",
        "No file in the repository was edited, and no commit or push was made"
      ]
    },
    {
      "id": 5,
      "prompt": "/triage-feedback --repo marigold-ui/insights 116",
      "expected_output": "PRECONDITION: insights PR 116 is open and authored by someone else. If it has since merged the run stops at step 1 on the merged check, which is correct behaviour but tests a different row, so the case is inconclusive rather than failed.\n\nThe target is in a different repository from the checkout, so no fix can land there whatever the authorship says. Step 1 resolves the repo from --repo rather than the remote, and the mode table's cross-repo row applies: replies and resolves are reachable, the code is not. Every gh call carries the repo explicitly, because GH_REPO does not override gh repo view and there is no --repo flag on it.",
      "files": [],
      "expectations": [
        "The repo is resolved from the --repo argument, not from the checkout's git remote",
        "The response states that no code will change because the PR is in another repository",
        "No file in the repository was edited, and no commit or push was made",
        "No pnpm command was run",
        "Every gh call that acts on the PR passes the repo explicitly rather than relying on the remote",
        "The triage table is rendered before any reply is posted or thread resolved"
      ]
    },
    {
      "id": 6,
      "prompt": "/triage-feedback 5781",
      "expected_output": "PRECONDITION: PR 5781 carries two threads from @aromko, both deliberately left unresolved after being replied to. The naming one because only half of it landed, the de-hardcoding but not the plugin move that would make the skill loadable in another repo. The GitLab one because it was pushed to a follow-up. Turn is theirs on both, since ours was the last word. If either has since been resolved the case is inconclusive rather than failed.\n\nThe case is about the Thread column. Neither row may be resolved: one is a partial apply and one is a push back, and apply on its own is not a licence to resolve. A run that resolves either has re-derived the decision from Action, which step 7 forbids. Turn theirs rows are reported as what the PR is waiting on, not replied to again.",
      "files": [],
      "expectations": [
        "The triage table carries a Thread value for every row, not only for some",
        "Neither of the two threads is resolved",
        "The naming thread is not resolved on the grounds that a fix was applied to it",
        "The count line states the number of resolves as zero",
        "The response says what each thread left open is still waiting on",
        "Rows whose Turn is theirs are reported as what the PR waits on rather than replied to again"
      ]
    },
    {
      "id": 7,
      "prompt": "/triage-feedback https://git.reservix.io/portal/main/-/merge_requests/11427",
      "expected_output": "A GitLab MR URL resolves the platform before anything else: project path `portal/main` (encoded `portal%2Fmain`), host `git.reservix.io` carried into every call as `--hostname`, iid 11427. The MR is open and authored by someone else, so the mode is respond-only, and it is a different repository from the checkout, so steps 5 and 6 never run.\n\nThe gather returns 6 discussions. Three are system notes (`added N commits`) and are dropped before triage. The remaining three are unresolved resolvable threads, so the table holds three rows and says the GitLab source ran alone.",
      "files": [],
      "expectations": [
        "The response identifies the target as a GitLab MR and names the project, the host and the iid",
        "The mode is respond-only, and the response does not state or imply that it will apply fixes",
        "No file in the repository was edited, and no commit or push was made",
        "System notes were dropped rather than triaged: no row describes a commit being added or a title being changed",
        "The table holds three rows, one per unresolved human thread",
        "The glab calls go through the path supplied by the rx-baseline gitlab skill, not a bare `glab` and not a hardcoded version-pinned path",
        "Every glab call carries --hostname rather than relying on the configured default",
        "The triage table is rendered before any reply is posted or discussion resolved",
        "No Vercel source was gathered, and the single-source table is not hedged as truncated"
      ]
    },
    {
      "id": 8,
      "prompt": "/triage-feedback --gitlab core/main 36586",
      "expected_output": "The short form resolves to the same target shape as a URL. MR 36586 is merged, and a merged MR stays merged, so this is a durable target rather than a sentence describing one.\n\nStep 1 stops on a merged or closed MR, before any gather. Resolving threads on a landed MR is noise, and on this platform there is not even a fix that could follow.",
      "files": [],
      "expectations": [
        "The response stops and says the MR is merged",
        "No discussion was gathered, no reply posted and no discussion resolved",
        "The stop happens in step 1, not after a triage table was rendered"
      ]
    },
    {
      "id": 9,
      "prompt": "/triage-feedback --gitlab core/main 36307 --vercel",
      "expected_output": "A GitLab target has exactly one source. Previews build from the GitHub repo, so an MR has no toolbar threads, and `--vercel` alongside `--gitlab` is a contradiction rather than a filter.\n\nThe skill says so in one line and stops. It does not silently drop the flag and run the GitLab half anyway, because a user who asked for Vercel feedback and got a GitLab-only table has been answered a question they did not ask.",
      "files": [],
      "expectations": [
        "The response refuses the flag combination and explains why in one line",
        "No gather was run on either source",
        "The response does not quietly ignore --vercel and proceed"
      ]
    },
    {
      "id": 10,
      "prompt": "/triage-feedback https://git.reservix.io/core/main/-/merge_requests/36307 (with Docker stopped)",
      "expected_output": "The wrapper refuses to start without Docker and says so: `Error: Docker is not running or not accessible.` That first `glab api user` call is the preflight, so the failure lands before any gather.\n\nThe skill passes the error text on verbatim and stops. There is no degraded mode on this platform: the wrapper is the only source a GitLab target has, so continuing would produce an empty table that looks like an MR with no feedback.",
      "files": [],
      "expectations": [
        "The response stops and reports the wrapper's own error text, including the remedy",
        "No triage table was rendered, and no empty table was presented as a finished pass",
        "The response does not fall back to a bare `glab` binary or to any other transport"
      ]
    },
    {
      "id": 11,
      "prompt": "/triage-feedback https://git.reservix.io/core/main/-/merge_requests/36307",
      "expected_output": "This MR is the case where both filters have to fire. It returns 9 discussions: 6 are system notes from GitLab's activity feed, and all 3 real threads already carry `resolved: true`. After dropping the first set and skipping the second, nothing is left to triage.\n\nThe right answer is an empty table and a plain statement that there is no open feedback, not a table of six rows describing commits that were pushed and a title that was changed.",
      "files": [],
      "expectations": [
        "The response reports no open feedback on the MR",
        "No system note appears as a triage row",
        "The three already-resolved threads were skipped rather than re-opened or replied to",
        "No reply was posted and no discussion resolved",
        "The response does not present the empty result as a failure or as a gather that went wrong"
      ]
    }
  ]
}
