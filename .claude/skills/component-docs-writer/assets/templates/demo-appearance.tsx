import type { ComponentProps } from '@marigold/components';
import { Component } from '@marigold/components';

export default (props: ComponentProps) => (
  <Component {...props}>
    {/* Handle different props/variants if needed */}
    Content
  </Component>
);
