'use client';

import { TextProps } from '@marigold/components';
import { Text } from '@marigold/components';

export default (props: TextProps) => (
  <Text {...props}>
    RUI is our transformative approach to creating an intuitive and
    user-friendly software experience for everyone.
  </Text>
);
