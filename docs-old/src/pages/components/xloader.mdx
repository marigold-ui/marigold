---
title: XLoader
group: Content
caption: Should be used for loading state.
github: https://github.com/marigold-ui/marigold/blob/main/packages/components/src/XLoader/XLoader.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/xloader.mdx
switchTheme: true
---

The `<XLoader>` component, is a simple `<SVG>` that should be used for loading states.
It should be displayed when the user is waiting for an upcoming event.

You can use all regular attributes of the `<SVG>`.

<Preview>
  <BasicXLoader />
</Preview>

## Usage

### Import

To import the component you just have to use this code below.

```tsx onlyCode
import { XLoader } from '@marigold/components';
```

### Appearance

<AppearanceTable component={title} />

### Props

<PropsTable
  props={[
    {
      property: 'fill',
      type: 'string',
      description: 'Set the color of the loader.',
      default: 'none',
    },
    {
      property: 'size',
      type: 'string | number',
      description: 'Set the width and height of the loader.',
      default: '150',
    },
    {
      property: '...',
      type: '',
      description: 'Yes! You can use all regular properties of svg!',
      default: '',
    },
  ]}
/>

## Examples

### Use case name

In this Example you can see how to use the `fill` and `size` property of the `<XLoader>`.

```tsx preview file=components/XLoader/XLoader-props.demo.tsx

```
