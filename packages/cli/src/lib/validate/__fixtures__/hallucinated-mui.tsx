import { Box, Container, Paper, Typography } from '@marigold/components';

export default () => (
  <Container>
    <Paper>
      <Typography variant="h1">Hello</Typography>
      <Box>Content</Box>
    </Paper>
  </Container>
);
