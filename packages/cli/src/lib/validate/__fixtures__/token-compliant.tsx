import { Button, Headline, Stack } from '@marigold/components';

const TokenCompliant = () => (
  <Stack space="small">
    <Headline level={2}>Token Compliance Test</Headline>
    <Button variant="primary">Themed Button</Button>
  </Stack>
);

export default TokenCompliant;
