import { Tabs } from '@marigold/components';

const InvalidTabs = () => (
  <Tabs>
    <Tabs.List>
      <Tabs.Item id="one">Only One</Tabs.Item>
    </Tabs.List>
  </Tabs>
);

export default InvalidTabs;
