import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Menu } from './Menu';
import { MenuItem } from '../MenuItem';
import { BurgerMenu } from '@marigold/icons';
import { useState } from 'react';


<Meta
  title="Components/Menu"
  parameters={{
    actions: {
      handles: ['click'],
    },
  }}
  argTypes={{
    variant: {
      control: {
        type: 'text',
      },
      description: '?',
      table: {
        defaultValue: {
          summary: 'menu',
        },
      },
    },
    label: {
      control: {
        type: 'text',
      },
      description: 'Menu label on hover',
      table: {
        defaultValue: {
          summary: 'Menu',
        },
      },
    },
    show: {
      control: {
        type: 'boolean',
      },
      options: [true, false],
      description: 'Show what',
      table: {
        defaultValue: {
          summary: false,
        },
      },
    },
    onClick: {
      control: {
        type: 'text',
      },
      description: 'Function to show menu or toggle something',
      type: { required: true },
    },
  }}
/>

# Menu

export const Template = ({ onClick, ...args }) => {
  const [showMenu, setShowMenu] = React.useState(false);
  return (
    <Menu onClick={() => setShowMenu(!showMenu)} show={showMenu} label={<BurgerMenu />} {...args}>
      <MenuItem href="#">Home</MenuItem>
      <MenuItem href="#">Tickets</MenuItem>
      <MenuItem href="#">Logout</MenuItem>
    </Menu>
  );
};

<Canvas>
  <Story name="Mini menu">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Mini menu" />
