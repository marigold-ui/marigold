import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Label } from './Label';

<Meta
  title="Components/Label"
  argTypes={{
    variant: {
      control: {
        type: 'text',
      },
      description: '?',
      table: {
        defaultValue: {
          summary: 'default',
        },
      },
    },
    htmlFor: {
      control: {
        type: 'text',
      },
      type: {
        required: true,
      },
    },
  }}
/>

# Label

export const Template = args => <Label htmlFor="input">Label</Label>;

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />
