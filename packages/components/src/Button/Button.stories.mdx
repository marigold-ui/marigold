import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Facebook, FormatSize, Id, Ticket } from '@marigold/icons';
import { Button } from './Button';
import { useStyles } from '@marigold/system';

<Meta title="Components/Button" />

### Large

<Canvas>
  <Story name="primary.large">
    <Button variant="primary" size="large">
      Primary
    </Button>
  </Story>
  <Story name="secondary.large">
    <Button variant="secondary" size="large">
      Secondary
    </Button>
  </Story>
  <Story name="ghost.large">
    <Button variant="ghost" size="large">
      Ghost
    </Button>
  </Story>
  <Story name="text.large">
    <Button variant="text" size="large">
      Text Only
    </Button>
  </Story>
</Canvas>

### Small

<Canvas>
  <Story name="primary.small">
    <Button variant="primary" size="small">
      Primary
    </Button>
  </Story>
  <Story name="secondary.small">
    <Button variant="secondary" size="small">
      Secondary
    </Button>
  </Story>
  <Story name="ghost.small">
    <Button variant="ghost" size="small">
      Ghost
    </Button>
  </Story>
  <Story name="text.small">
    <Button variant="text" size="small">
      Text Only
    </Button>
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="primary.large.disabled">
    <Button variant="primary" size="large" disabled>
      Primary Large Disabled
    </Button>
  </Story>
  <Story name="secondary.large.disabled">
    <Button variant="secondary" size="large" disabled>
      Secondary Large Disabled
    </Button>
  </Story>
  <Story name="ghost.large.disabled">
    <Button variant="ghost" size="large" disabled>
      Ghost Large Disabled
    </Button>
  </Story>
  <Story name="text.large.disabled">
    <Button variant="text" size="large" disabled>
      Text Only Large Disabled
    </Button>
  </Story>
</Canvas>

### Default with icon

<Canvas>
  <Story name="primary.large with default icon">
    <Button icon={<Ticket />}>Icon Primary</Button>
  </Story>
  <Story name="primary.small with default icon">
    <Button size="small" icon={<Ticket size={20} />}>
      Icon Primary
    </Button>
  </Story>
  <Story name="secondary.large with default icon">
    <Button variant="secondary" icon={<Facebook />}>
      Like me!
    </Button>
  </Story>
  <Story name="secondary.small with default icon">
    <Button variant="secondary" size="small" icon={<Facebook size={20} />}>
      Like me!
    </Button>
  </Story>
  <Story name="ghost.large with default icon">
    <Button variant="ghost" icon={<Id />}>
      Ghost
    </Button>
  </Story>
  <Story name="ghost.small with default icon">
    <Button variant="ghost" size="small" icon={<Id size={20} />}>
      Ghost
    </Button>
  </Story>
  <Story name="text.large with default icon">
    <Button variant="text" icon={<FormatSize />}>
      Icon Text only
    </Button>
  </Story>
  <Story name="text.small with default icon">
    <Button variant="text" size="small" icon={<FormatSize size={20} />}>
      Icon Text only
    </Button>
  </Story>
</Canvas>

### Large with large icon

<Canvas>
  <Story name="primary.large with large icon">
    <Button variant="primary" size="large">
      <Facebook size={48} /> Like me!
    </Button>
  </Story>
</Canvas>

### Small with small icon

<Canvas>
  <Story name="primary.small with small icon">
    <Button variant="primary" size="small">
      <Facebook size={16} /> Like me!
    </Button>
  </Story>
</Canvas>

### With colored icon

<Canvas>
  <Story name="with color">
    <Button variant="primary" size="small">
      <Facebook fill="black" /> Like me!
    </Button>
  </Story>
</Canvas>

### Custom styled

<Canvas>
  <Story name="custom">
    <Button
      variant="secondary"
      size="small"
      className={useStyles({
        css: { bg: '#3b5998' },
      })}
    >
      <Facebook /> Like me!
    </Button>
  </Story>
</Canvas>
