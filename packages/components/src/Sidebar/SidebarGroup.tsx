import type { ReactNode } from 'react';

export interface SidebarGroupLabelProps {
  children?: ReactNode;
}

export const SidebarGroupLabel = (_props: SidebarGroupLabelProps) => {
  return null;
};

SidebarGroupLabel.__SIDEBAR_GROUP_LABEL__ = true;
