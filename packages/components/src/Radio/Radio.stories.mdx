import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Label, Radio } from '@marigold/components';
import { useState } from 'react';

<Meta title="Components/Radio" />

# Radio Button

## Description

With the Radio Button component you can add a HTML `<input>` element with `type="radio"` to your form.
The element uses always the themeSection with the name: `form` in your given theme. The variant in your section can be added with the variant prop. The default variant is `radio`.
Specific css style prop can also be added.

## Properties

| Property  | Type        | Default   |
| :-------- | :---------- | :-------- |
| `variant` | `string`    | `'radio'` |
| `css`     | `css props` |           |

## Import

```tsx
import { Radio } from '@marigold/components';
```

## Usage

### Radio standard labeled

<Preview>
  <Story name="Radio">
    <div>
      <Label htmlFor="mc">
        <Radio id="mc" name="payment" value="Mastercard" />
        Mastercard
      </Label>
      <br />
      <Label htmlFor="vi">
        <Radio id="vi" name="payment" value="Visa" />
        Visa
      </Label>
      <br />
      <Label htmlFor="ae">
        <Radio id="ae" name="payment" value="AmericanExpress" />
        American Express
      </Label>
    </div>
  </Story>
</Preview>

### Radio checked and disabled

<Preview>
  <Story name="RadioTwo">
    <div>
      <Radio checked />
      <Radio disabled />
      <Radio checked disabled />
    </div>
  </Story>
</Preview>

export const [state, setState] = React.useState('one');
export const onChange = ev => {
  setState(ev.currentTarget.value);
};

<Preview>
  <Story name="RadioThree">
    <div>
      <Radio
        name="label"
        onChange={onChange}
        checked={state === 'one'}
        value="one"
        onClick={() => alert('asfasd')}
      />
      <Radio
        name="label"
        onChange={onChange}
        checked={state === 'two'}
        value="two"
      />
      <Radio
        name="label"
        onChange={onChange}
        checked={state === 'three'}
        value="three"
      />
    </div>
  </Story>
</Preview>
