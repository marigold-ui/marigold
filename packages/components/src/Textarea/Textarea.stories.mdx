import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Textarea } from '../Textarea';

<Meta
  title="Components/Textarea"
  argTypes={{
    variant: {
      control: {
        type: 'text',
      },
      table: {
        defaultValue: {
          summary: 'default',
        },
      },
    },
    label: {
      control: {
        type: 'text',
      },
      description: 'Text above',
    },
    htmlFor: {
      control: {
        type: 'text',
      },
      description: 'Bind to label',
      table: {
        defaultValue: {
          summary: 'textarea',
        },
      },
    },
    errorMessage: {
      control: {
        type: 'text',
      },
      description: 'Error if errorMessage is given',
    },
    required: {
      control: {
        type: 'boolean',
      },
      table: {
        defaultValue: {
          summary: false,
        },
      },
    },
  }}
/>

# Textarea

export const Template = args => (
  <Textarea placeholder="Placeholder..." {...args} />
);

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />
