/**
 * NOTE: We currently have to augment `Theme` with
 * the index property until we have typed all components.
 */
// declare module '@marigold/system' {
//   export interface Theme {
//     // [index: string]: any;
//   }
// }
