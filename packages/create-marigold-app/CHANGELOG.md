# create-marigold-app

## 0.2.0

### Minor Changes

- 6fb5791: New package: Claude Code plugin for scaffolding Marigold apps. Install via `claude plugin marketplace add github:marigold-ui/marigold --sparse packages/create-marigold-app` then `claude plugin install create-marigold-app`.
