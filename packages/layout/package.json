{
  "name": "@marigold/layout",
  "version": "0.0.0",
  "description": "Layout components for the Marigold Design System",
  "main": "dist/index.js",
  "module": "dist/layout.esm.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.x",
    "react-dom": "^16.x"
  },
  "devDependencies": {
    "@marigold/system": "0.0.0"
  },
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json --transpileOnly",
    "watch": "tsdx watch --tsconfig tsconfig.build.json --transpileOnly"
  }
}
