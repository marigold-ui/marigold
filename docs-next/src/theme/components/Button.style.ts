import type { Theme } from '@marigold/components';

export const Button: Theme['components']['Button'] = {
  base: {},
  variant: {
    navigationSmall: {
      border: 'none',
      outline: 'none',
    },
  },
};
