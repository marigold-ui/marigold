---
title: List
group: Content
---

Component to list things. Renders a `ul` by default, but can also render an `ol`.

## Props | Property | Type | Default | | :------------------- |

:------------------ | :------ | | `variant` (optional) | `string ⎮ string[]` | |

## Usage
