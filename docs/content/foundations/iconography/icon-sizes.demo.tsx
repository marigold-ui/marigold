import { Search } from '@marigold/icons';

export default () => (
  <div className="flex items-end gap-6">
    <Search size={16} />
    <Search size={20} />
    <Search size={24} />
    <Search size={32} />
  </div>
);
