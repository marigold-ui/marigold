import { Button, Inline } from '@marigold/components';

export default () => (
  <Inline space="related" alignX="center" alignY="center">
    <Button variant="primary">Save Changes</Button>
    <Button variant="secondary">Cancel</Button>
  </Inline>
);
