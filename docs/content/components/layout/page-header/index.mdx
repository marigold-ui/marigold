---
title: Page.Header
description: The title, description, and primary actions at the top of a Page.
badge: beta
---

`<Page.Header>` is the header of a [`<Page>`](/components/layout/page). Like [`<Panel.Header>`](/components/layout/panel), it is a slot-context provider: it lays out a [`<Title>`](/components/content/title), an optional [`<Description>`](/components/content/description), and optional actions in a two-column grid. It has no sub-components of its own — you compose the shared `<Title>` / `<Description>` and action primitives directly.

```tsx
<Page>
  <Page.Header>
    <Title>Team members</Title>
    <Description>Invite and manage who has access.</Description>
    <ActionButton variant="primary">Invite member</ActionButton>
  </Page.Header>
  {/* … */}
</Page>
```

<ComponentDemo name="page-header" background="page" />

## Usage

### Title and description

The `<Title>` renders as the page `<h1>` (the level comes from `<Page>`'s `headingLevel`, which defaults to `1`). The `<Description>` renders as a paragraph beneath it. The `<main>` landmark is labelled by the title automatically.

### Actions

Page actions use the slot-aware action primitives, the same set `<Panel.Header>` accepts: [`<ActionButton>`](/components/actions/action-button), [`<ActionGroup>`](/components/actions/action-group), and [`<ActionMenu>`](/components/overlay/action-menu). Unlike a Panel — whose header actions are low-emphasis icon chrome — a page's primary action is prominent, so reach for `<ActionButton variant="primary">`.

<GuidelineTiles>
  <Do>
    <DoDescription>
      Use `<ActionButton variant="primary">` for the page's primary action so it
      claims the header's action slot and reads as the page-level call to
      action.
    </DoDescription>
  </Do>
  <Dont>
    <DontDescription>
      Don't drop a bare `<Button>` into `<Page.Header>` — it isn't slot-aware
      and won't pick up the header's grid placement. Use the action primitives
      above.
    </DontDescription>
  </Dont>
</GuidelineTiles>

## Props

<StorybookHintMessage component={'Page'} />

### Page.Header

<AutoTypeTable path="Page/PageHeader" name="PageHeaderProps" />

## Related components

<ul>
  <li>
    [Page](/components/layout/page): The content area that owns the `main`
    landmark, padding, and rhythm.
  </li>
  <li>
    [Panel](/components/layout/panel): A titled surface with the same header
    composition model.
  </li>
</ul>
