---
title: Card
description: Represents one item in a collection.
---

import { CardAnatomy } from './card-anatomy';
import {
  DoListMockup,
  DoNestedMockup,
  DoTableMockup,
  DoTextMockup,
  DontListMockup,
  DontNestedMockup,
  DontTableMockup,
  DontTextMockup,
} from './card-usage-guidelines';

A `<Card>` represents one item in a collection an event preview, a venue tile, a product summary that repeats next to similar siblings in a grid or list. Each card groups related content (image, title, body, actions) into a single, self-contained unit and sets it apart from the page with a raised shadow.

## Anatomy

The `<Card>` component is a compound component made up of several sub-components that map to distinct visual areas of the card.

<CardAnatomy />

- **Card.Preview**: Optional full-bleed area at the top, typically used for images or media.
- **Card.Header**: Area for the card title and supporting labels or badges.
- **Card.Body**: Main content area for descriptive text or form elements.
- **Card.Footer**: Bottom area for actions such as buttons or metadata.

## Appearance

<AppearanceDemo component={'Card'} />

<AppearanceTable component={'Card'} />

The card component supports variants for general content grouping as well as access-level marking.

| Variant   | Description                                                      | When to use                                                                                      |
| --------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `default` | Standard card with a raised shadow. This is the default variant. | General-purpose content grouping like articles, profiles, or summaries.                          |
| `admin`   | Card with admin-specific border and background tint.             | Grouping content visible only to admin users. [Learn more.](/patterns/layout/admin-master-mark)  |
| `master`  | Card with master-specific border and background tint.            | Grouping content visible only to master users. [Learn more.](/patterns/layout/admin-master-mark) |

## Usage

A card represents one item in a set. Reach for it when the same type of item repeats in a grid or list events, products, venues, team members and each entry links to its own detail view. Repetition is the core case if the element doesn't repeat, a card is probably the wrong tool.

When the content doesn't fit that shape, reach for one of these instead:

- [`<Table>`](../table) for records that share the same shape and need sorting, scanning, or bulk actions.
- [`<Panel>`](../../layout/panel) for a named, non-repeating page region like "Billing" or "Team members".
- [`<Headline>`](../../content/headline) + [`<Text>`](../../content/text) for a short paragraph or single field that doesn't need framing.
- [`<SectionMessage>`](../../content/section-message) for a status, callout, or feedback message.
- [`<Stack>`](../../layout/stack) with [`<Divider>`](../../content/divider) for a long list of similar rows without media or actions.

### Card grids

The canonical card layout is a grid of items, each with its own visual identity, that wraps responsively as the viewport changes. Combine card with [`<Tiles>`](../../layout/tiles) so the grid reflows automatically.

<ComponentDemo name="card-grid" />

The same shape works for any repeating content team directories with avatars, article previews with author metadata, or category pickers with no media at all. The identity-per-card pattern stays the same only the contents of each slot change.

### Media

`Card.Preview` is optional. Whether to include an image depends on whether the visual carries information.

Use an image preview when each item has its own visual identity an event poster, a venue photo, a product hero. The image is part of how users recognize and choose between cards in a grid.

<ComponentDemo name="card-elevation" />

<GuidelineTiles>
  <Do>
    <DoDescription>
      Put images in `Card.Preview` so they sit full-bleed at the top of the
      card.
    </DoDescription>
  </Do>
  <Dont>
    <DontDescription>
      Don't nest the card's main visual inside `Card.Body`. The padding cuts the
      image off from the card's edges and the framing falls apart.
    </DontDescription>
  </Dont>
</GuidelineTiles>

Drop `Card.Preview` when the content is text-driven a plan summary, a status block, or a quick stat with actions. A title and short body carry the meaning, and a generic stock image only adds noise without helping users decide.

<ComponentDemo name="card-minimal" />

### Footer actions

Put actions in `Card.Footer` when users should be able to act on the item directly from the grid, without opening a detail page.

Use small buttons (`<Button size="small" />`). Cards sit in tight grid cells, and small buttons match the scale of the surrounding content. Reserve default-size buttons for page-level actions outside of cards.

### When not to use a card

The four common misuses below cover the shapes that get mistaken for card most often.

#### Tabular data

When records share the same shape and users want to scan, sort, or compare across them, a [`<Table>`](../table) reads faster and handles selection, sorting, and bulk actions out of the box. A column of cards forces the eye to jump between visual blocks instead of running down a column of values.

<GuidelineTiles>
  <Do>
    <DoFigure>
      <DoTableMockup />
    </DoFigure>
    <DoDescription>
      Use a [`<Table>`](../table) when records share the same shape and users
      want to scan, sort, or compare them.
    </DoDescription>
  </Do>
  <Dont>
    <DontFigure>
      <DontTableMockup />
    </DontFigure>
    <DontDescription>
      Don't render homogeneous, row-shaped data as a column of cards. Users lose
      the ability to scan and compare values at a glance.
    </DontDescription>
  </Dont>
</GuidelineTiles>

#### Simple content

A card groups several pieces of related content. If you only have a single paragraph, a short message, or one field, the card adds visual weight without grouping anything. Use a [`<Headline>`](../../content/headline), [`<Text>`](../../content/text), or [`<SectionMessage>`](../../content/section-message) instead.

<GuidelineTiles>
  <Do>
    <DoFigure>
      <DoTextMockup />
    </DoFigure>
    <DoDescription>
      Render simple, single-purpose content as plain text. Use a
      [`<SectionMessage>`](../../content/section-message) when the content is a
      status or callout.
    </DoDescription>
  </Do>
  <Dont>
    <DontFigure>
      <DontTextMockup />
    </DontFigure>
    <DontDescription>
      Don't wrap a single short paragraph or one field in a card. The frame
      adds visual noise without grouping anything.
    </DontDescription>
  </Dont>
</GuidelineTiles>

#### Long lists of cards

Cards rely on contrast against the page to feel grouped. When you stack a long column of full-width cards, every row competes with every other row, the shadows pile up, and the page becomes visually noisy. Use a list separated by [`<Divider>`](../../content/divider) for similar, row-shaped content instead.

<GuidelineTiles>
  <Do>
    <DoFigure>
      <DoListMockup />
    </DoFigure>
    <DoDescription>
      Use a list with [`<Divider>`](../../content/divider) between rows for
      long, homogeneous content.
    </DoDescription>
  </Do>
  <Dont>
    <DontFigure>
      <DontListMockup />
    </DontFigure>
    <DontDescription>
      Don't stack a long column of full-width cards. The repeated shadows and
      borders fragment the page and harm scannability.
    </DontDescription>
  </Dont>
</GuidelineTiles>

#### Nested cards

Cards already sit on a raised surface. Nesting a card inside another card creates double elevation that no longer reads as depth, it's just a frame inside a frame. Restructure the inner content with a layout primitive like [`<Stack>`](../../layout/stack), a [`<Divider>`](../../content/divider), or split the content into sibling sections.

<GuidelineTiles>
  <Do>
    <DoFigure>
      <DoNestedMockup />
    </DoFigure>
    <DoDescription>
      Restructure the inner content with [`<Stack>`](../../layout/stack) and
      [`<Divider>`](../../content/divider) so a single card holds the sections.
    </DoDescription>
  </Do>
  <Dont>
    <DontFigure>
      <DontNestedMockup />
    </DontFigure>
    <DontDescription>
      Don't nest a card inside another card, place it inside a
      [`<Panel>`](../../layout/panel), or sit it on any other same-elevation
      surface. The shadow has nothing to push against.
    </DontDescription>
  </Dont>
</GuidelineTiles>

## Props

<StorybookHintMessage component={'Card'} />

### Card

<AutoTypeTable path="Card" name="CardProps" />

### Card.Preview

<AutoTypeTable path="Card/CardPreview" name="CardPreviewProps" />

### Card.Header

<AutoTypeTable path="Card/CardHeader" name="CardHeaderProps" />

### Card.Body

<AutoTypeTable path="Card/CardBody" name="CardBodyProps" />

### Card.Footer

<AutoTypeTable path="Card/CardFooter" name="CardFooterProps" />

## Related

<TeaserList
  items={[
    {
      title: 'Panel',
      href: '../../layout/panel',
      caption:
        'The page-region counterpart to card. Use Panel for named, non-repeating sections like "Billing" or "Team members".',
      icon: (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth={2}
          strokeLinecap="round"
          strokeLinejoin="round"
          className="size-5"
        >
          <rect x="3" y="3" width="18" height="18" rx="2" />
          <path d="M3 9h18" />
        </svg>
      ),
    },
    {
      title: 'Elevation',
      href: '../../../foundations/elevation',
      caption:
        'This page should introduce you on how to use elevation with Marigold.',
      icon: (
        <svg
          viewBox="0 0 24 24"
          className="size-6"
          strokeWidth="1.5"
          stroke="currentColor"
        >
          <path d="M3.5625 6.375H20.4375V8.25H3.5625V6.375ZM3.5625 16.125H20.4375V18H3.5625V16.125ZM20.4375 11.25H3.5625V13.125H20.4375V11.25Z"></path>
        </svg>
      ),
    },
    {
      title: 'Admin- & master mark',
      href: '/patterns/layout/admin-master-mark',
      caption: 'Used for marking internal-only features.',
      icon: (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width={24}
          height={24}
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth={2}
          strokeLinecap="round"
          strokeLinejoin="round"
          className="size-5"
        >
          <path d="M9 11l-6 6v3h9l3-3" />
          <path d="M22 12l-4.6 4.6a2 2 0 01-2.8 0l-5.2-5.2a2 2 0 010-2.8L14 4" />
        </svg>
      ),
    },
  ]}
/>
