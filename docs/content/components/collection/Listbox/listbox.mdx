---
title: ListBox
caption: A component for displaying a list of options and allows user to select one or more of them.
badge: new
---

The `<ListBox>` helps you to build accessible listbox of components that can be styled as needed.

`<ListBox>` component consists of a container element, with a list of items or sections inside. Users can select one or more items by clicking, tapping, or navigating with the keyboard.

## Usage

### Import

To import the component you just have to use this code below.

```tsx
import { ListBox } from '@marigold/components';
```

### Appearance

<AppearanceTable component={title} />

### ListBox Props

<PropsTable
  props={[
    {
      property: 'selectionMode',
      type: 'none, single, multiple',
      description: 'The type of selection that is allowed in the collection.',
      default: 'none',
    },
    {
      property: 'onSelectionChange',
      type: '(keys: Selection) => any',
      description: 'Handler that is called when the selection changes.',
      default: 'none',
    },
    {
      property: 'disabledKeys',
      type: 'all | Iterable<Key> | undefined',
      description: 'Disabled list items',
      default: 'none',
    },
    {
      property: 'selectedKeys',
      type: 'all | Iterable<Key> | undefined',
      description:
        'The currently selected keys in the collection (controlled).',
      default: 'none',
    },
    {
      property: 'dependencies',
      type: 'any[] | undefined',
      description:
        'Values that should invalidate the item cache when using dynamic collections.',
      default: 'none',
    },
    {
      property: 'disallowEmptySelection',
      type: 'boolean | undefined',
      description: 'Whether the collection allows empty selection.',
      default: 'none',
    },
    {
      property: 'items',
      type: 'Iterable<object> | undefined',
      description: 'Item objects in the collection.',
      default: 'none',
    },
    {
      property: 'selectionBehavior',
      type: 'Iterable<object> | undefined',
      description: 'How multiple selection should behave in the collection.',
      default: 'none',
    },
    {
      property: 'ref',
      type: 'React.Ref<HTMLUListElement> | undefined',
      description:
        'Allows getting a ref to the component instance. Once the component unmounts, React will set ref.current to null (or call the ref with null if you passed a callback ref).',
      default: 'none',
    },
    {
      property: 'onAction',
      type: '((key: Key) => void) | undefined',
      description:
        "Handler that is called when a user performs an action on an item. The exact user event depends on the collection's selectionBehavior prop and the interaction modality.",
      default: 'none',
    },
    {
      property: 'onBlur',
      type: '((e: React.FocusEvent<Element, Element>) => void) | undefined',
      description: 'Handler that is called when the element loses focus.',
      default: 'none',
    },
    {
      property: 'onFocus',
      type: '((e: React.FocusEvent<Element, Element>) => void) | undefined',
      description: 'Handler that is called when the element receives focus.',
      default: 'none',
    },
    {
      property: 'onFocusChange',
      type: '((isFocused: boolean) => void) | undefined',
      description:
        "Handler that is called when the element's focus status changes.",
      default: 'none',
    },
    {
      property: 'onScroll',
      type: '((e: React.UIEvent<HTMLDivElement, UIEvent>) => void) | undefined',
      description: 'Handler that is called when a user scrolls',
      default: 'none',
    },
  ]}
/>

### Items Props

<PropsTable
  props={[
    {
      property: 'id',
      type: 'Key | undefined',
      description: 'The unique id of the item.',
      default: 'none',
    },
    {
      property: 'download',
      type: 'string | boolean | undefined',
      description:
        'Causes the browser to download the linked URL. A string may be provided to suggest a file name.',
      default: 'none',
    },
    {
      property: 'href',
      type: 'string | undefined',
      description: 'A URL to link to.',
      default: 'none',
    },
    {
      property: 'ping',
      type: 'string | undefined',
      description:
        'A space-separated list of URLs to ping when the link is followed.',
      default: 'none',
    },
    {
      property: 'referrerPolicy',
      type: 'React.HTMLAttributeReferrerPolicy | undefined',
      description: 'How much of the referrer to send when following the link.',
      default: 'none',
    },
    {
      property: 'rel',
      type: 'string | undefined',
      description:
        'The relationship between the linked resource and the current page. ',
      default: 'none',
    },
    {
      property: 'target',
      type: 'React.HTMLAttributeAnchorTarget | undefined',
      description: 'The target window for the link.',
      default: 'none',
    },
    {
      property: 'textValue',
      type: 'string | undefined',
      description:
        "A string representation of the item's contents, used for features like typeahead.",
      default: 'none',
    },
    {
      property: 'value',
      type: 'object | undefined',
      description:
        'The object value that this item represents. When using dynamic collections, this is set automatically.',
      default: 'none',
    },
  ]}
/>

## Examples

### Simple ListBox

In this example you can see a simple `<ListBox>` with some items to select .

<ComponentDemo file="./listbox-basic.demo.tsx" />

### MultiSelection ListBox

In this example you can select multiple list items.

<ComponentDemo file="./listbox-multiselection.demo.tsx" />

### Simple with Sections

This example shows how to use `<ListBox>` with `<ListBox.Section>` .

<ComponentDemo file="./listbox-sections.demo.tsx" />
