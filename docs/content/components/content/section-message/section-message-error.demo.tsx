import { SectionMessage, Text } from '@marigold/components';

export default () => (
  <SectionMessage variant="error" messageTitle="Wrong here.">
    <Text>Hello, I am a simple error message.</Text>
  </SectionMessage>
);
