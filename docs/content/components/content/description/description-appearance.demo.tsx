import type { DescriptionProps } from '@marigold/components';
import { Description } from '@marigold/components';

export default (props: DescriptionProps) => (
  <Description {...props}>
    A description sits next to a Title or label and gives the user a brief
    explanation of what they are looking at.
  </Description>
);
