import type { TitleProps } from '@marigold/components';
import { Title } from '@marigold/components';

export default (props: TitleProps) => (
  <Title {...props}>Your Tickets for Summer Festival 2025</Title>
);
