---
title: Header
group: Content
switch: true
---

## Description

The `Header` component is used to help structure content components. It's used like the HTML `<header>` tag.

```tsx preview file=components/header/header.demo.tsx

```

## Props

<PropsTable
  props={[
    {
      property: 'variant',
      type: 'string',
      description: 'Use a different _variant_ from theme',
      default: 'none',
    },
    {
      property: 'size',
      type: 'string',
      description: 'Use a different _size_ from theme',
      default: 'none',
    },
  ]}
/>

## Examples

```tsx preview file=components/header/header.demo.tsx

```
