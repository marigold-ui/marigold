---
title: ActionGroup
---

import { FigmaLink } from '../../src/components/FigmaLink';

# ActionGroup

## Description

The ActionGroup component adds two or more components together to form a common group.

<FigmaLink
  to={
    'https://www.figma.com/file/DFKyTGHAoDxOsUBPszLLxP/%F0%9F%8F%B5%EF%B8%8FMarigold?node-id=864%3A1'
  }
/>

## Properties

| Property                       | Type                           | Default |
| :----------------------------- | :----------------------------- | :------ |
| `space` (optional)             | `ResponsiveStyleValue<string>` | `none`  |
| `verticalAlignment` (optional) | `boolean`                      | `false` |

## Import

```tsx onlyCode
import { ActionGroup } from '@marigold/components';
```

## Live-Code Example

```tsx expandCode
<ActionGroup space="xsmall">
  <Button variant="text" size="small">
    Cancel
  </Button>
  <Button variant="primary" size="small">
    Confirm
  </Button>
</ActionGroup>
```

## Usage

```tsx
<ActionGroup>
  <Button variant="text" size="small">
    Cancel
  </Button>
  <Button variant="secondary" size="small">
    Delete <Delete />
  </Button>
</ActionGroup>
```

### With space

```tsx
<ActionGroup space="small">
  <Button variant="ghost">Back</Button>
  <Button variant="secondary">Next</Button>
</ActionGroup>
```

### With vertical alignment

```tsx
<ActionGroup space="small" verticalAlignment>
  <Button variant="ghost" size="small">
    Sort <SortUp />
  </Button>
  <Button variant="secondary" size="small">
    Sort <SortDown />
  </Button>
</ActionGroup>
```
