---
title: Textarea
---

import { FigmaLink } from '../../src/components/FigmaLink';

# Textarea

The Textarea component consists of an `<textarea>` element and a `<label>`.

<MarigoldTheme>
  <Textarea
    label="Textarea One"
    htmlFor="textareaOne"
    placeholder="Place your text here.."
  />
</MarigoldTheme>

```tsx onlyCode
import { Textarea } from '@marigold/components';
```

<FigmaLink
  to={
    'https://www.figma.com/file/DFKyTGHAoDxOsUBPszLLxP/%F0%9F%8F%B5%EF%B8%8FMarigold?node-id=454%3A1'
  }
/>

## Props

| Property                  | Type      | Default     |
| :------------------------ | :-------- | :---------- |
| `variant` (optional)      | `string`  | `__default` |
| `label`                   | `string`  |             |
| `htmlFor`                 | `string`  |             |
| `error` (optional)        | `boolean` |             |
| `errorMessage` (optional) | `string`  |             |
| `required` (optional)     | `boolean` |             |

## Examples

Default Textarea

```tsx expandCode
<Textarea
  label="Textarea One"
  htmlFor="textareaOne"
  placeholder="Place your text here.."
/>
```

Disabled Textarea

```tsx
<Textarea
  label="Textarea Three"
  htmlFor="textareaThree"
  placeholder="Placeholder disabled"
  disabled
/>
```

Required Textarea

```tsx
<Textarea label="Required Textarea" htmlFor="required" required />
```

Error Textarea

```tsx
<Textarea
  label="Required Textarea with error"
  htmlFor="error"
  required
  error
  errorMessage="Oh no, we have an error!"
/>
```
