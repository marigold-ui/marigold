---
title: Aside
group: Layout
---

The `<Aside>` layout is useful when you want to set two things next to each other and the one has a fixed width (the aside) and the other can have a dynamic width (the content).

It is important to note that the `<Aside>` must have exactly too children, where none is a `<Fragment>`.

Here you can switch the theme of the component.

```tsx preview
<Aside space="medium-1">
  <Box border="1px solid #ced4da" bg="#e9ecef" height="100px" width="50px" />
  <Box border="1px solid #ced4da" bg="#e9ecef" />
</Aside>
```

```tsx
import { Aside } from '@marigold/components';
```

## Props

<PropsTable
  props={[
    {
      property: 'space',
      type: 'ResponsiveStyleValue<string>',
      description: 'The space between the content and the aside.',
      default: 'none',
    },
    {
      property: 'side',
      type: '"left" | "right"',
      description: 'The side of the content.',
      default: 'left',
    },
    {
      property: 'sideWidth',
      type: 'ResponsiveStyleValue<string>',
      description: 'The side with of the content.',
      default: 'none',
    },
    {
      property: 'wrap',
      type: 'Non-zero Percentage',
      description: 'The wrap of the content.',
      default: '50%',
    },
    {
      property: 'strech',
      type: 'boolean',
      description: 'strech the aside.',
      default: 'true',
    },
  ]}
></PropsTable>

## Examples

Space between elements

```tsx preview
<Aside space="medium">
  <Box border="1px solid #ced4da" bg="#e9ecef" height="100px" width="50px" />
  <Box border="1px solid #ced4da" bg="#e9ecef" />
</Aside>
```

Aside on the right

```tsx preview
<Aside space="medium" side="right">
  <Box border="1px solid #ced4da" bg="#e9ecef" />
  <Box border="1px solid #ced4da" bg="#e9ecef" height="100px" width="50px" />
</Aside>
```

## Do and Don'ts
