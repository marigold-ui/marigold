---
title: Radio
group: Forms
figma: https://www.figma.com/file/DFKyTGHAoDxOsUBPszLLxP/%F0%9F%8F%B5%EF%B8%8FMarigold?node-id=467%3A332
switch: true
---

The `Radio` and `Radio.Group` components allow users to select one option from a set of options. They are useful for presenting a list of options to users and collecting their responses.

<Preview>
  <Radio.Group label="Choose desert" defaultValue="apple">
    <Radio value="ice cream">🍨 Ice Cream</Radio>
    <Radio value="cupcake">🧁 Cupcake</Radio>
    <Radio value="cookie" disabled>
      🍪 Cookie (sold out)
    </Radio>
    <Radio value="apple">🍎 Apple</Radio>
  </Radio.Group>
</Preview>

```tsx onlyCode
import { Radio } from '@marigold/components';
```

to view the design of the component click on the link below

## Props

<PropsTable
  props={[
    {
      property: 'variant',
      type: 'string',
      description: 'Use a different _variant_ from theme',
      default: 'none',
    },
    {
      property: 'size',
      type: 'string',
      description: 'Use a different _size_ from theme',
      default: 'none',
    },
    {
      property: 'error',
      type: 'boolean',
      description:
        'If `true`, the checkbox is considered invalid and if set the `errorMessage` is shown instead of the `description`.',
      default: 'false',
    },
    {
      property: 'width',
      type: 'string',
      description: 'Control the width of the field',
      default: '100%',
    },
  ]}
/>

## Examples

### Simple Radio

```tsx preview file=components/Radio/radio.demo.tsx

```

### Disabed Radio

```tsx preview file=components/Radio/radio-disabled.demo.tsx

```

### Radio with required label

```tsx preview file=components/Radio/radio-required.demo.tsx

```
