import { DateField } from '@marigold/components';

export default () => (
  <DateField
    label="Event Date Selection"
    error
    errorMessage="Unable to select a date. Please try again or contact support."
  />
);
