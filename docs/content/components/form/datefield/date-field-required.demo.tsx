import { DateField } from '@marigold/components';

export default () => (
  <DateField
    label="Booking Deadline"
    required
    description="Please select the deadline for booking. This field is required to finalize your ticket reservation."
  />
);
