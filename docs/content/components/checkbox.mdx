---
title: Checkbox
---

import { FigmaLink } from '../../src/components/FigmaLink';

import { DoAndDont } from '../../src/components/DoAndDont';
import {
  Box,
  Card,
  Checkbox,
  Columns,
  Column,
  Inline,
  Stack,
  Text,
} from '@marigold/components';

# Checkbox

## Demo

<MarigoldTheme>
  <Inline space="medium">
    <Checkbox id="unchecked" aria-label="unchecked" />
    <Checkbox id="disabled" aria-label="disabled" disabled />
    <Checkbox id="checked" aria-label="checked" checked />
    <Checkbox
      id="disabled-checked"
      aria-label="disabled-checked"
      checked
      disabled
    />
    <Checkbox
      id="indeterminated-checked"
      aria-label="indeterminated-checked"
      checked
      indeterminated
    />
  </Inline>
</MarigoldTheme>

## Description

Checkboxes allow users to select multiple items from a list of individual items,
or to mark one individual item as selected.

<FigmaLink
  to={
    'https://www.figma.com/file/DFKyTGHAoDxOsUBPszLLxP/%F0%9F%8F%B5%EF%B8%8FMarigold?node-id=467%3A159'
  }
/>

## Properties

| Property                    | Type      | Default     |
| :-------------------------- | :-------- | :---------- |
| `id`                        | `string`  |             |
| `variant` (optional)        | `string`  | `__default` |
| `labelVariant` (optional)   | `string`  | `inline`    |
| `required` (optional)       | `boolean` | `false`     |
| `disabled` (optional)       | `boolean` | `false`     |
| `indeterminated` (optional) | `boolean` | `false`     |
| `error` (optional)          | `boolean` | `false`     |
| `errorMessage` (optional)   | `string`  |             |

## Import

```tsx onlyCode
import { Checkbox } from '@marigold/components';
```

## Types

### Checkbox standard labeled

```tsx expandCode
() => {
  const [isChecked, setChecked] = React.useState(false);
  const onChange = () => {
    setChecked(!isChecked);
  };
  return (
    <Checkbox
      id="standard"
      name="standard"
      onChange={onChange}
      checked={isChecked}
      value="Checkbox"
    >
      Agree to Marigold terms
    </Checkbox>
  );
};
```

### Checkbox disabled

```tsx
<>
  <Checkbox id="disabled" disabled>
    Disabled
  </Checkbox>
  <br />
  <Checkbox id="checkedDisabled" checked disabled>
    Checked and disabled
  </Checkbox>
</>
```

### Checkbox required label

```tsx
<Checkbox checked id="required" value="required" required>
  Agree to Marigold terms
</Checkbox>
```

### Checkbox error and errorMessage

Use the error and errorMesssage prop together

```tsx
<Checkbox
  id="error"
  value="error"
  error
  errorMessage="Please check the required checkbox"
  required
>
  Agree to Marigold terms
</Checkbox>
```

### Indeterminated Checkbox

Use indeterminate state when it represents both selected and not selected values.

```tsx
<Checkbox id="selectAll" checked indeterminated>
  Select all
</Checkbox>
```

## Usage

<Stack space="small">
  <Box
    display="grid"
    css={{
      gridGap: 'small',
      gridTemplateColumns: 'repeat(auto-fit, minmax(min(500px, 100%), 1fr))',
    }}
  >
    <DoAndDont
      preview={
        <Card>
          <Stack>
            <Checkbox id="blue" checked>
              Blue
            </Checkbox>
            <Checkbox id="red" checked>
              Red
            </Checkbox>
            <Checkbox id="yellow">Yellow</Checkbox>
          </Stack>
        </Card>
      }
    >
      Use checkboxes to allow users to select one or more options from a list.
    </DoAndDont>
    <DoAndDont
      preview={
        <Card>
          <Stack space="medium">
            <Checkbox id="select" value="error" checked indeterminated>
              Select all colors
            </Checkbox>
            <Stack>
              <Checkbox id="blue" checked>
                Blue
              </Checkbox>
              <Checkbox id="red" checked>
                Red
              </Checkbox>
              <Checkbox id="yellow">Yellow</Checkbox>
            </Stack>
          </Stack>
        </Card>
      }
    >
      Use a checkbox to select all values. When multiple values that are not
      identical are represented, the checkbox should appear in the indeterminate
      state.
    </DoAndDont>
    <DoAndDont
      dont
      preview={
        <Card>
          <Columns space="small">
            <Column width={9}>Activate Option Number 1</Column>
            <Column width={3}>
              <Checkbox id="option" checked aria-label="option" />
            </Column>
            <Column width={9}>Create a Backup</Column>
            <Column width={3}>
              <Checkbox id="backup" checked aria-label="backup" />
            </Column>
            <Column width={9}>Allow password changing</Column>
            <Column width={3}>
              <Checkbox id="pw" checked aria-label="pw" />
            </Column>
          </Columns>
        </Card>
      }
    >
      Do not use checkboxes to replace a switch.
    </DoAndDont>
    <DoAndDont
      dont
      preview={
        <Card>
          <Text as="h5" variant="headline5">
            Select your answer
          </Text>
          <Columns space="small">
            <Column width={9}>Yes</Column>
            <Column width={3}>
              <Checkbox id="yes" checked aria-label="yes" />
            </Column>
            <Column width={9}>No</Column>
            <Column width={3}>
              <Checkbox id="no" checked aria-label="no" />
            </Column>
            <Column width={9}>I am not sure</Column>
            <Column width={3}>
              <Checkbox id="not-sure" checked aria-label="not-sure" />
            </Column>
          </Columns>
        </Card>
      }
    >
      Do not use checkboxes to replace a radio.
    </DoAndDont>
  </Box>
</Stack>
