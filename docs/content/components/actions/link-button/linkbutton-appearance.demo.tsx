import type { LinkButtonProps } from '@marigold/components';
import { LinkButton } from '@marigold/components';

export default (props: LinkButtonProps) => (
  <LinkButton {...props}>Press me</LinkButton>
);
