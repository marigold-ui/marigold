---
title: Field
---

# Field

## Description

With the Field component you can add a HTML `<label>` and `<input>` element to your form.

## Properties

| Property                  | Type                                                                                       | Default |
| :------------------------ | :----------------------------------------------------------------------------------------- | :------ |
| `htmlFor`                 | `string`                                                                                   |         |
| `label`                   | `string`                                                                                   |         |
| `required` (optional)     | `boolean`                                                                                  |         |
| `error` (optional)        | `boolean`                                                                                  |         |
| `errorMessage` (optional) | `string`                                                                                   |         |
| `disabled` (optional)     | `boolean`                                                                                  |         |
| `type`                    | `date, datetime-local, email, month, number, password, search, tel, text, time, url, week` | `text`  |

## Import

```tsx code
import { Field } from '@marigold/components';
```

## Live-Code Example

```tsx live
<Field label="This is my field:" htmlFor="id" />
```

## Usage

### Standard Field

```tsx
<Field label="Firstname" htmlFor="myId1" />
```

### Disabled Field

```tsx
<Field label="Field disabled" htmlFor="myId6" disabled />
```

### Required Field

```tsx
<Field label="Name" htmlFor="myId2" required />
```

### Error Field

```tsx
<Field label="Name" htmlFor="myId3" error errorMessage="Validation message" />
```

### Field Examples

```tsx
<Field label="Choose the week" type="week" htmlFor="myId4" />
```

```tsx
<Field
  label="Pick your date"
  type="date"
  htmlFor="myId5"
  error
  errorMessage="ERROR!"
/>
```
