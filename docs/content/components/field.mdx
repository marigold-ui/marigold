---
title: Field
---

# Field

## Description

With the Field component you can add a HTML `<label>` and `<input>` element to your form.
The variant can be added with the variant prop. The default variant is `default`.

## Properties

| Property             | Type                                                                                       | Default   |
| :------------------- | :----------------------------------------------------------------------------------------- | :-------- |
| `variant` (optional) | `string`                                                                                   | `default` |
| `htmlFor`            | `string`                                                                                   |           |
| `label`              | `string`                                                                                   |           |
| `error` (optional)   | `string`                                                                                   |           |
| `type`               | `date, datetime-local, email, month, number, password, search, tel, text, time, url, week` | `text`    |

## Import

```tsx code
import { Field } from '@marigold/components';
```

## Live-Code Example

```tsx live
<Field label="This is my field:" htmlFor="id" />
```

## Usage

### Input type text

```tsx
<Field label="Firstname" htmlFor="myId" />
```

### Input type text with error

```tsx
<Field label="Name" htmlFor="myId" error="Validation message" />
```

### Another input type

```tsx
<Field label="Choose the week" type="week" htmlFor="myId" />
```

### Another input type with error

```tsx
<Field label="Pick your date" type="date" htmlFor="myId" error="ERROR!" />
```
