import { Stack } from '@/ui';
import { Headline } from '@marigold/components';

const InventoryPage = () => (
  <Stack space={8}>
    <Headline level={1}>Inventory</Headline>
  </Stack>
);

export default InventoryPage;
