---
title: Image
group: Content
---

With the Image component you can render an image element.
The variant can be added with the variant prop. You can also put the standard html image props like `alt` and `src`.

```tsx
import { Image } from '@marigold/components';
```

## Usage

### Apperance

<AppearanceTable component={title} />

### Props

<PropsTable
  props={[
    {
      property: 'alt',
      type: 'string',
      description: '',
      default: '',
    },
    {
      property: 'fit',
      type: 'TODO!',
      description: '',
      default: '',
    },
    {
      property: 'position',
      type: 'TODO!',
      description: '',
      default: '',
    },
  ]}
/>

## Usage

```tsx preview file=components/Image/image.demo.tsx

```
