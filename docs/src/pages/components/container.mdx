---
title: Container
group: Layout
---

Containers width is based on used font and is optimized for readability. You can use `header` or `content` as content type and set the `size` prop to `small`, `medium` or `large`.

```tsx preview file=components/Container/basic-container.demo.tsx

```

## Props

<PropsTable
  props={[
    {
      property: 'contentType',
      description: 'The content type of the container',
      type: '"header", "content"',
      default: '"content"',
    },
    {
      property: 'size',
      description: 'The size of the container',
      type: '"small", "medium", "large"',
      default: '"medium"',
    },
    {
      property: 'align',
      description: 'set alignment the content inside the container',
      type: '"left", "center", "right"',
      default: '"left"',
    },
    {
      property: 'alignItems',
      description: 'set alignment of the items inside the container',
      type: '"left", "center", "right"',
      default: '"left"',
    },
  ]}
/>

## Examples

### Container header small, medium & large

```tsx preview file=components/Container/small-header-container.demo.tsx

```

```tsx preview file=components/Container/medium-header-container.demo.tsx

```

```tsx preview file=components/Container/large-header-container.demo.tsx

```

### Container content small, medium & large

```tsx preview file=components/Container/small-content-container.demo.tsx

```

```tsx preview file=components/Container/large-content-container.demo.tsx

```

## Text Container

In some cases, the width of the content can be smaller than the container's width. You can use the align prop to align the content left, center or right.

```tsx preview file=components/Container/full-container.demo.tsx

```
