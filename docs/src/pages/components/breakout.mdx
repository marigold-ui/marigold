---
title: Breakout
group: Layout
github: https://github.com/marigold-ui/marigold/blob/main/packages/components/src/Breakout/Breakout.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/breakout.mdx
caption: Component that breaks out of a grid layout context.
---

`<Breakout>` is a layout component which lets you span the full row in a CSS grid context e.g. our [`<Container>`](container/) component. You need this component if you want to display something out of the context.

You can use `horizontalAlign` or `verticalAlign` to place the items inside the `<Breakout>` element.

<Preview>
  <Container align="center">
    <Box
      css={{
        border: '1px solid #ced4da',
        bg: '#e9ecef',
        height: '80px',
        width: '100%',
      }}
    />
    <Breakout>
      <Box
        css={{
          bg: '#e9ecef',
          border: '1px solid #ced4da',
          textAlign: 'center',
        }}
      >
        BREAKOUT
      </Box>
    </Breakout>
    <Box
      css={{
        border: '1px solid #ced4da',
        bg: '#e9ecef',
        height: '80px',
        width: '100%',
      }}
    />
  </Container>
</Preview>

## Usage

### Import

To import the component you just have to use this code below.

```tsx
import { Breakout } from '@marigold/components';
```

### Props

<PropsTable
  props={[
    {
      property: 'horizontalAlign',
      type: '"top" | "bottom" | "center"',
      description:
        'Horizontal alignment of the items inside the breakout element',
      default: 'center',
    },
    {
      property: 'verticalAlign',
      type: '"left" | "center" | "right"',
      description:
        'Vertical alignment of the items inside the breakout element',
      default: 'center',
    },
  ]}
/>

## Examples

### Vertical alignment

```tsx preview file=components/Breakout/vertical-breakout.demo.tsx

```

### Horizontal alignment

```tsx preview file=components/Breakout/horizontal-breakout.demo.tsx

```

### Breakout Text inside Container

```tsx preview file=components/Breakout/text-breakout.demo.tsx

```

### Breakout iframe inside Text Container

```tsx preview file=components/Breakout/iframe-breakout.demo.tsx

```
