---
title: SVG
group: Content
caption: SVG wrapper
github: https://github.com/marigold-ui/marigold/blob/main/packages/system/src/components/SVG/SVG.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/svg.mdx
---

With the `<SVG>` component you can pass SVG paths in it. It behaves like the usual `<svg>` element.

You can use all known properties from the regular `<svg>` element.

<Preview>
  <BasicSVG />
</Preview>

## Usage

### Import

To import the component you just have to use this code below.

```tsx onlyCode
import { SVG } from '@marigold/system';
```

### Props

<PropsTable
  props={[
    {
      property: 'size',
      type: 'number | string | number[] | string[]',
      description: 'The size of the SVG element.',
      default: '24',
    },
    {
      property: 'fill',
      type: 'string',
      description: 'The color of the SVG element.',
      default: 'currentColor',
    },
    {
      property: 'css',
      type: 'CSSObject',
      description: 'Pass in regular CSS',
      default: 'none',
    },
    {
      property: '...',
      type: '',
      description: 'Yes you can use all regular attributes of svg!',
      default: '',
    },
  ]}
/>

## Examples

### Simple SVG

In this example you can see how to use the `<SVG>` element.

```tsx preview file=components/SVG/svg.demo.tsx

```
