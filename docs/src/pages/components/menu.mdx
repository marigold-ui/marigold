---
title: Menu
group: Overlay
github: https://github.com/marigold-ui/marigold/blob/main/packages/components/src/Menu/Menu.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/menu.mdx
caption: Component for building a menu.
switchTheme: true
badge: 'updated'
---

The `<Menu>` component allows you to define a menu element. It's useful when you want a list with options or actions.

It is structured in three parts: `<Menu.Trigger>`, `<Menu>` and `<Menu.Item>`. And there is a simple `<Button>` necessary for handling the menu trigger.

If you want create a variant, you can style different parts of the `<Menu>` separate, there is the `container` part and the `item`.

There is also a new Component called `<ActionMenu>` which you can use if you want to take some actions. You can have a look on how it works in the examples. It works quiet similar to the normal `<Menu>` component. All you have to add are the `<Menu.Item>`s.

<Preview>
  <Menu.Trigger>
    <Button variant="menu" size="small">
      Choose Menu
    </Button>
    <Menu>
      <Menu.Item key="burger">🍔 Burger</Menu.Item>
      <Menu.Item key="pizza">🍕 Pizza</Menu.Item>
      <Menu.Item key="salad">🥗 Salad</Menu.Item>
      <Menu.Item key="fries">🍟 Fries</Menu.Item>
    </Menu>
  </Menu.Trigger>
</Preview>

## Usage

### Apperance

<AppearanceTable component={title} />

### Props

<PropsTable
  props={[
    {
      property: 'onAction',
      type: '(key: Key) => void',
      description: 'Handler that should call an action.',
      default: 'none',
    },
  ]}
/>

### Trigger props

<PropsTable
  props={[
    {
      property: 'disabled',
      type: 'boolean',
      description: 'Disables the complete menu.',
      default: 'false',
    },
  ]}
/>

## Examples

### Simple Menu with Action

In this example you can see a simple `<Menu>` with some items to select. After selection an action can be applied.

```tsx preview file=components/Menu/menu-action.demo.tsx

```

### Disabled Menu

In this example the `<Menu.Trigger>` has set its prop `disabled`. So you can't interact with the `<Menu>` anymore.

```tsx preview file=components/Menu/menu-disabled.demo.tsx

```

### ActionMenu

In this example, an `Icon` has been inserted into the `Button` to serve as a kebab menu.
As shown here, any type of icon can be used within the menu and there are no restrictions.
It is very similar to the normal `<Menu>` component.

```tsx preview file=components/Menu/kebab-menu.demo.tsx

```
