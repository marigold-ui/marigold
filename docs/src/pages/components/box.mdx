---
title: Box
group: Layout
switchTheme: true
github: https://github.com/marigold-ui/marigold/blob/main/packages/system/src/components/Box/Box.tsx
edit: https://github.com/marigold-ui/marigold/blob/main/docs/src/pages/components/box.mdx
caption: The all-rounder among the components.
badge: updated
---

The `<Box>` is a basic component like the HTML div element. You can modify the component with different style props. It is also possible to write `__baseCSS` and `css` styles on the `<Box>`.

You can use the `as` prop to let the `<Box>` behave as any Marigold component or HTML Element with all its properties.

For a more detailed description, see [Box Primitive](../../foundation/box-primitive).

<Preview>
  <Box border="1px solid gray" p="small" borderRadius="5px">
    I am basic div box!
  </Box>
</Preview>

## Usage

### Import

To import the component you just have to use this code below.

```tsx
import { Box } from '@marigold/components';
```

### Props

<PropsTable
  props={[
    {
      property: 'as',
      type: 'HTMLElement | MarigoldComponent',
      description:
        'Behaves like a HTMLElement or a Marigold component if used.',
      default: 'div',
    },
    {
      property: 'variant',
      type: 'string⎮string[]',
      description: 'The variant of the component.',
      default: 'none',
    },
    {
      property: '__baseCSS',
      type: 'CSSObject',
      description: 'Style the base CSS.',
      default: 'none',
    },
    {
      property: 'css',
      type: 'CSSObject | CSSObject',
      description: 'Set CSS for the component.',
      default: '',
    },
  ]}
/>

## Examples

### Basic Box

In this example the `<Box>` is modified with several props.

```tsx preview file=components/Box/box-success.demo.tsx

```

### Box as Button

The `as` prop is set to a button HTMLElement on the left side and to a Marigold button on the right.

```tsx preview file=components/Box/button-box.demo.tsx

```

### Box with several props

How to use different props and CSS on the `<Box>`.

```tsx preview file=components/Box/box-text.demo.tsx

```
